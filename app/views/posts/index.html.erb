<h2>Blog Posts</h2>
<% @posts.each do |post| %>
    <div clas="well">
    <h3><%= post.title %></h3>
    <p><%= post.body %></p>
    <%= link_to "Read More", post_path(post), :class => 'btn btn-default' %>
    </div>
<% end %> 
  